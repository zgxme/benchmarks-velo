ALTER ACCOUNT
SET
  use_cached_result = FALSE;

CREATE OR REPLACE TABLE LINEORDER 
(
    LO_ORDERKEY      NUMBER(18,0) NOT NULL,
    LO_LINENUMBER    NUMBER(18,0) NOT NULL,
    LO_CUSTKEY       NUMBER(18,0) NOT NULL,
    LO_PARTKEY       NUMBER(18,0) NOT NULL,
    LO_SUPPKEY       NUMBER(18,0) NOT NULL,
    LO_ORDERDATE     NUMBER(18,0) NOT NULL,
    LO_ORDERPRIORITY VARCHAR(16) NOT NULL,
    LO_SHIPPRIORITY  NUMBER(18,0) NOT NULL,
    LO_QUANTITY      NUMBER(18,0) NOT NULL,
    LO_EXTENDEDPRICE NUMBER(18,0) NOT NULL,
    LO_ORDTOTALPRICE NUMBER(18,0) NOT NULL,
    LO_DISCOUNT      NUMBER(18,0) NOT NULL,
    LO_REVENUE       NUMBER(18,0) NOT NULL,
    LO_SUPPLYCOST    NUMBER(18,0) NOT NULL,
    LO_TAX           NUMBER(18,0) NOT NULL,
    LO_COMMITDATE    NUMBER(18,0) NOT NULL,
    LO_SHIPMODE      VARCHAR(11) NOT NULL
)
CLUSTER BY (LO_ORDERDATE, LO_CUSTKEY);

CREATE OR REPLACE TABLE CUSTOMER 
(
    C_CUSTKEY    NUMBER(18,0) NOT NULL,
    C_NAME       VARCHAR(26) NOT NULL,
    C_ADDRESS    VARCHAR(41) NOT NULL,
    C_CITY       VARCHAR(11) NOT NULL,
    C_NATION     VARCHAR(16) NOT NULL,
    C_REGION     VARCHAR(13) NOT NULL,
    C_PHONE      VARCHAR(16) NOT NULL,
    C_MKTSEGMENT VARCHAR(11) NOT NULL
);

CREATE OR REPLACE TABLE SUPPLIER 
(
    S_SUPPKEY    NUMBER(18,0) NOT NULL,
    S_NAME       VARCHAR(26) NOT NULL,
    S_ADDRESS    VARCHAR(26) NOT NULL,
    S_CITY       VARCHAR(11) NOT NULL,
    S_NATION     VARCHAR(16) NOT NULL,
    S_REGION     VARCHAR(13) NOT NULL,
    S_PHONE      VARCHAR(16) NOT NULL
);

CREATE OR REPLACE TABLE PART 
(
    P_PARTKEY    NUMBER(18,0) NOT NULL,
    P_NAME       VARCHAR(23) NOT NULL,
    P_MFGR       VARCHAR(7) NOT NULL,
    P_CATEGORY   VARCHAR(8) NOT NULL,
    P_BRAND      VARCHAR(10) NOT NULL,
    P_COLOR      VARCHAR(12) NOT NULL,
    P_TYPE       VARCHAR(26) NOT NULL,
    P_SIZE       NUMBER(18,0) NOT NULL,
    P_CONTAINER  VARCHAR(11) NOT NULL
);
CREATE OR REPLACE TABLE DATES 
(
    D_DATEKEY         NUMBER(18,0) NOT NULL,
    D_DATE            VARCHAR(20) NOT NULL,
    D_DAYOFWEEK       VARCHAR(10) NOT NULL,
    D_MONTH           VARCHAR(11) NOT NULL,
    D_YEAR            NUMBER(18,0) NOT NULL,
    D_YEARMONTHNUM    NUMBER(18,0) NOT NULL,
    D_YEARMONTH       VARCHAR(9) NOT NULL,
    D_DAYNUMINWEEK    NUMBER(18,0) NOT NULL,
    D_DAYNUMINMONTH   NUMBER(18,0) NOT NULL,
    D_DAYNUMINYEAR    NUMBER(18,0) NOT NULL,
    D_MONTHNUMINYEAR  NUMBER(18,0) NOT NULL,
    D_WEEKNUMINYEAR   NUMBER(18,0) NOT NULL,
    D_SELLINGSEASON   VARCHAR(14) NOT NULL,
    D_LASTDAYINWEEKFL NUMBER(18,0) NOT NULL,
    D_LASTDAYINMONTHFL NUMBER(18,0) NOT NULL,
    D_HOLIDAYFL       NUMBER(18,0) NOT NULL,
    D_WEEKDAYFL       NUMBER(18,0) NOT NULL
)
CLUSTER BY (D_DATEKEY);

CREATE OR REPLACE TABLE LINEORDER_FLAT
(
    LO_ORDERDATE     NUMBER(18,0) NOT NULL,
    LO_ORDERKEY      NUMBER(18,0) NOT NULL,
    LO_LINENUMBER    NUMBER(18,0) NOT NULL,
    LO_CUSTKEY       NUMBER(18,0) NOT NULL,
    LO_PARTKEY       NUMBER(18,0) NOT NULL,
    LO_SUPPKEY       NUMBER(18,0) NOT NULL,
    LO_ORDERPRIORITY VARCHAR(100) NOT NULL,
    LO_SHIPPRIORITY  NUMBER(18,0) NOT NULL,
    LO_QUANTITY      NUMBER(18,0) NOT NULL,
    LO_EXTENDEDPRICE NUMBER(18,0) NOT NULL,
    LO_ORDTOTALPRICE NUMBER(18,0) NOT NULL,
    LO_DISCOUNT      NUMBER(18,0) NOT NULL,
    LO_REVENUE       NUMBER(18,0) NOT NULL,
    LO_SUPPLYCOST    NUMBER(18,0) NOT NULL,
    LO_TAX           NUMBER(18,0) NOT NULL,
    LO_COMMITDATE    DATE NOT NULL,
    LO_SHIPMODE      VARCHAR(100) NOT NULL,
    C_NAME           VARCHAR(100) NOT NULL,
    C_ADDRESS        VARCHAR(100) NOT NULL,
    C_CITY           VARCHAR(100) NOT NULL,
    C_NATION         VARCHAR(100) NOT NULL,
    C_REGION         VARCHAR(100) NOT NULL,
    C_PHONE          VARCHAR(100) NOT NULL,
    C_MKTSEGMENT     VARCHAR(100) NOT NULL,
    S_NAME           VARCHAR(100) NOT NULL,
    S_ADDRESS        VARCHAR(100) NOT NULL,
    S_CITY           VARCHAR(100) NOT NULL,
    S_NATION         VARCHAR(100) NOT NULL,
    S_REGION         VARCHAR(100) NOT NULL,
    S_PHONE          VARCHAR(100) NOT NULL,
    P_NAME           VARCHAR(100) NOT NULL,
    P_MFGR           VARCHAR(100) NOT NULL,
    P_CATEGORY       VARCHAR(100) NOT NULL,
    P_BRAND          VARCHAR(100) NOT NULL,
    P_COLOR          VARCHAR(100) NOT NULL,
    P_TYPE           VARCHAR(100) NOT NULL,
    P_SIZE           NUMBER(18,0) NOT NULL,
    P_CONTAINER      VARCHAR(100) NOT NULL
)
CLUSTER BY (LO_ORDERDATE, LO_ORDERKEY);