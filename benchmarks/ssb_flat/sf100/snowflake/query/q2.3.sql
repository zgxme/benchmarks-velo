SELECT
    sum(LO_REVENUE),
    FLOOR(LO_ORDERDATE / 10000) AS year,
    P_BRAND
FROM LINEORDER_FLAT
WHERE P_BRAND = 'MFGR#2239' AND S_REGION = 'EUROPE'
GROUP BY
    year,
    P_BRAND
ORDER BY
    year,
    P_BRAND;